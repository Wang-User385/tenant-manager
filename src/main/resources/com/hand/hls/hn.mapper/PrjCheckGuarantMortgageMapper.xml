<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hand.hls.hn.mapper.PrjCheckGuarantMortgageMapper">
    <resultMap id="BaseResultMap" type="com.hand.hls.hn.dto.PrjCheckGuarantMortgage">
        <result column="guarant_mortgage_id" property="guarantMortgageId" jdbcType="DECIMAL" />
        <result column="check_id" property="checkId" jdbcType="DECIMAL" />
        <result column="lease_situation_remark" property="leaseSituationRemark" jdbcType="VARCHAR" />
        <result column="mortgage_situation_remark" property="mortgageSituationRemark" jdbcType="VARCHAR" />
        <result column="contract_number" property="contractNumber" jdbcType="VARCHAR" />
        <result column="project_id" property="projectId" jdbcType="DECIMAL" />
    </resultMap>
    <select id="queryList" parameterType="com.hand.hls.hn.dto.PrjCheckGuarantMortgage" resultMap="BaseResultMap">
        select * from
        (SELECT
        (select pcc.guarant_mortgage_id
        from PRJ_CHECK_GUARANT_MORTGAGE pcc
        where pcc.CHECK_ID = #{checkId}) guarant_mortgage_id,
        (select pcc.lease_situation_remark
        from PRJ_CHECK_GUARANT_MORTGAGE pcc
        where pcc.CHECK_ID = #{checkId}) lease_situation_remark,
        p.check_id,
        c.project_id,
        c.tenant_id bp_id,
        (SELECT bp_name FROM hls_bp_master hl WHERE hl.bp_id = c.tenant_id) bp_name,
        c.contract_number,
        p.check_number
        FROM
        prj_check p, con_contract c
        WHERE
        p.contract_id = c.contract_id) t
        <where>
            1=1
            <if test="checkId!=null">
                and t.CHECK_ID = #{checkId}
            </if>
        </where>
    </select>

</mapper>
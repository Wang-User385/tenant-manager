<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hand.hls.fnd.mapper.CockpitImportMapper">
    <resultMap id="BaseResultMap" type="com.hand.hls.fnd.dto.CockpitImport">
        <result column="COCKPIT_ID" property="cockpitId" jdbcType="DECIMAL"/>
        <result column="YEAR" property="year" jdbcType="DECIMAL"/>
        <result column="MON" property="mon" jdbcType="DECIMAL"/>
        <result column="ITEM" property="item" jdbcType="VARCHAR"/>
        <result column="BUS_ONE_AMOUNT" property="busOneAmount" jdbcType="DECIMAL"/>
        <result column="BUS_TWO_AMOUNT" property="busTwoAmount" jdbcType="DECIMAL"/>
        <result column="BUS_THREE_AMOUNT" property="busThreeAmount" jdbcType="DECIMAL"/>
        <result column="BUS_TD_AMOUNT" property="busTdAmount" jdbcType="DECIMAL"/>
        <result column="TOTAL_AMOUNT" property="totalAmount" jdbcType="DECIMAL"/>
        <result column="COMPLETED_AMOUNT_OD" property="completedAmountOd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_RATIO_OD" property="completedRatioOd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_AMOUNT_TD" property="completedAmountTd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_RATIO_TD" property="completedRatioTd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_AMOUNT_THD" property="completedAmountThd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_RATIO_THD" property="completedRatioThd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_AMOUNT_ZD" property="completedAmountZd" jdbcType="DECIMAL"/>
        <result column="COMPLETED_RATIO_ZD" property="completedRatioZd" jdbcType="DECIMAL"/>
        <result column="TOTAL_COMPLETED_AMOUNT" property="totalCompletedAmount" jdbcType="DECIMAL"/>
        <result column="TOTAL_COMPLETED_RATIO" property="totalCompletedRatio" jdbcType="DECIMAL"/>

        <result column="bp_id" property="bpId"/>
        <result column="host_project_manager" property="hostProjectManager"/>
        <result column="host_project_manager_n" property="hostProjectManagerN"/>
        <result column="bp_id_n" property="bpIdN"/>
        <result column="bp_name" property="bpName"/>
        <result column="group_customers" property="groupCustomers"/>
        <result column="group_customers_n" property="groupCustomersN"/>
        <result column="host_unit_id" property="hostUnitId"/>
        <result column="host_unit_id_n" property="hostUnitIdN"/>
        <result column="economic_indu_classify" property="economicInduClassify"/>
        <result column="economic_indu_classify_n" property="economicInduClassifyN"/>
        <result column="launch_total_amount" property="launchTotalAmount"/>
        <result column="scale_total_amount" property="scaleTotalAmount"/>
        <result column="income_total_amount" property="incomeTotalAmount"/>
        <result column="interest_total_amount" property="interestTotalAmount"/>
        <result column="charge_total_amount" property="chargeTotalAmount"/>
        <result column="cost_total_amount" property="costTotalAmount"/>
        <result column="profit_total_amount" property="profitTotalAmount"/>

        <result column="completed_launch_amount" property="completedLaunchAmount"/>
        <result column="launch_amount" property="launchAmount"/>
        <result column="launch_ratio" property="launchRatio"/>
        <result column="completed_bearing_amount" property="completedBearingAmount"/>
        <result column="bearing_amount" property="bearingAmount"/>
        <result column="bearing_ratio" property="bearingRatio"/>
        <result column="completed_income_amount" property="completedIncomeAmount" />
        <result column="income_amount" property="incomeAmount"/>
        <result column="income_ratio" property="incomeRatio"/>
        <result column="completed_profit_amount" property="completedProfitAmount"/>
        <result column="profit_amount" property="profitAmount"/>
        <result column="profit_ratio" property="profitRatio"/>
        <result column="authorization_flag" property="authorizationFlag"/>
        <result column="sum_due_amount" property="sumDueAmount"/>
        <result column="sum_credit_amount" property="sumCreditAmount"/>
        <result column="year_initial_amount" property="yearInitialAmount"/>
        <result column="received_principal" property="receivedPrincipal"/>
        <result column="received_interest" property="receivedInterest"/>
        <result column="redisual_amount" property="redisualAmount"/>

    </resultMap>
    <select id="queryPrjAuthorization" resultMap="BaseResultMap" parameterType="com.hand.hls.fnd.dto.CockpitImport">
        SELECT
            (
                CASE
                WHEN (
                    SELECT
                        count(1)
                    FROM
                        sys_user_allocation sa,
                        fnd_employee_assigns ea,
                        fnd_org_position fp
                    WHERE
                        ea.EMPLOYEE_ASSIGN_ID = sa.EMPLOYEE_ASSIGN_ID
                    AND ea.POSITION_ID = fp.POSITION_ID
                    AND fp.POSITION_CODE IN (
                        'A0430',
                        'A0440',
                        'A0450',
                        'A0510'
                    )
                    AND sa.user_id = #{hostProjectManager}
                ) > 0 THEN
                    'Y'
                WHEN (
                    SELECT
                        count(1)
                    FROM
                        sys_user_allocation sa,
                        fnd_employee_assigns ea,
                        fnd_org_position fp
                    WHERE
                        ea.EMPLOYEE_ASSIGN_ID = sa.EMPLOYEE_ASSIGN_ID
                    AND ea.POSITION_ID = fp.POSITION_ID
                    AND fp.POSITION_CODE IN (
                        'A0300',
                        'A0310',
                        'A0320',
                        'A0390'
                    )
                    AND sa.user_id = #{hostProjectManager}
                ) > 0 THEN
                    'N'
                ELSE
                    'All'
                END
            ) authorization_flag
        FROM
            DUAL
    </select>
    <select id="queryAll" resultMap="BaseResultMap" parameterType="com.hand.hls.fnd.dto.CockpitImport">
        select * from jc_cockpit_import t
        <where>
            1=1
            <if test="year!=null and year!=''">
                and t.year = #{year}
            </if>
            <if test="mon!=null and mon!=''">
                and t.mon = #{mon}
            </if>
            <if test="item!=null and item!=''">
                and t.item like concat(concat('%',#{item}),'%')
            </if>

        </where>
    </select>
    <select id="queryBpAllReport" resultMap="BaseResultMap" parameterType="com.hand.hls.fnd.dto.CockpitImport">
        SELECT
        t1.*, (
        t1.income_total_amount - t1.cost_total_amount
        ) profit_total_amount
        FROM
        (
        SELECT
        b.bp_id,
        b.bp_name,
        b.bp_name AS bp_id_n,
        b.GROUP_CUSTOMERS,
        t. YEAR,
        t.mon,
        (
        SELECT
        scvb.meaning
        FROM
        sys_code_b scb,
        sys_code_value_b scvb
        WHERE
        scb. CODE = 'SYS.YES_NO'
        AND scb.code_id = scvb.code_id
        AND scvb.
        VALUE
        = b.group_customers
        ) AS GROUP_CUSTOMERS_N,
        (
        SELECT DISTINCT
        wm_concat(DISTINCT p.HOST_UNIT_ID)
        FROM
        prj_project p
        WHERE
        p.TENANT_ID = b.bp_id
        ) HOST_UNIT_ID,
        (
        SELECT DISTINCT
        wm_concat(DISTINCT u.UNIT_NAME)
        FROM
        prj_project p,
        fnd_org_unit u
        WHERE
        p.TENANT_ID = b.bp_id
        AND p.HOST_UNIT_ID = u.UNIT_ID
        ) HOST_UNIT_ID_n,
        (
        SELECT DISTINCT
        wm_concat(
        DISTINCT p.HOST_PROJECT_MANAGER
        )
        FROM
        prj_project p
        WHERE
        p.TENANT_ID = b.bp_id
        ) HOST_PROJECT_MANAGER,
        (
        SELECT DISTINCT
        wm_concat(DISTINCT s.DESCRIPTION)
        FROM
        prj_project p,
        sys_user s
        WHERE
        p.TENANT_ID = b.bp_id
        AND p.HOST_PROJECT_MANAGER = s.user_id
        ) HOST_PROJECT_MANAGER_n,
        b.economic_indu_classify,
        nvl(
        (
        SELECT
        sc.meaning value_name
        FROM
        sys_code_value_b_v sc
        WHERE
        sc. CODE = 'FND.INDUSTRY_2'
        AND sc.code_value = b.economic_indu_classify
        ),
        (
        SELECT
        t.description
        FROM
        hls_stat_class t
        WHERE
        t.class_usage = 'BIZ_PARTNER'
        AND t.enabled_flag = 'Y'
        AND t.CLASS_ID = b.economic_indu_classify
        )
        ) economic_indu_classify_n,
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND cc.TENANT_ID = b.bp_id
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND c.tenant_id = b.bp_id
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        sum(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.bp_id = b.bp_id
        AND ac.document_type = 'REFUND'
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) launch_total_amount,
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND cc.TENANT_ID = b.bp_id
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND c.tenant_id = b.bp_id
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        sum(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.bp_id = b.bp_id
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        )
        ) scale_total_amount,
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) income_total_amount,
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 10
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) interest_total_amount,
        (
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) charge_total_amount,
        (
        (
        SELECT
        nvl(sum(
        nvl(
        (
        SELECT
        nvl(SUM(ccc.PRINCIPAL), 0)
        FROM
        con_contract_cashflow ccc
        WHERE
        ccc.contract_id = c.contract_id
        AND ccc.cf_item IN (1, 11)
        AND ccc.times >= cc.times
        AND ccc.cf_status = 'RELEASE'
        ),
        0
        ) * nvl(
        (
        SELECT
        (
        DATEDIFF(cc.due_date , MAX(ccf.due_date))
        ) / 360
        FROM
        con_contract_cashflow ccf
        WHERE
        ccf.contract_id = cc.contract_id
        AND ccf.times &lt; cc.times
        AND ccf.cf_item IN (1, 0)
        AND ccf.cf_status = 'RELEASE'
        ),
        0
        ) *  nvl(c.COM_COST_RATE, 0)
        ),0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) + (
        SELECT
        nvl(SUM(
        nvl(
        (
        SELECT
        nvl(SUM(ccc.NET_PRINCIPAL), 0)
        FROM
        con_contract_cashflow ccc
        WHERE
        ccc.contract_id = c.contract_id
        AND ccc.cf_item IN (1, 11)
        AND ccc.times >= cc.times
        AND ccc.cf_status = 'RELEASE'
        ),
        0
        ) * nvl(
        (
        SELECT
        (
        DATEDIFF(cc.due_date , MAX(ccf.due_date))
        ) / 360
        FROM
        con_contract_cashflow ccf
        WHERE
        ccf.contract_id = cc.contract_id
        AND ccf.times &lt; cc.times
        AND ccf.cf_item IN (1, 0)
        AND ccf.cf_status = 'RELEASE'
        ),
        0
        ) *  nvl(c.COM_COST_RATE, 0)
        ) ,0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.tenant_id = b.bp_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        )
        ) cost_total_amount
        FROM
        hls_bp_master b,
        (
        SELECT
        t. YEAR,
        t.mon
        FROM
        jc_cockpit_import t
        GROUP BY
        t. YEAR,
        t.mon
        ) t
        WHERE
        EXISTS (
        SELECT
        1
        FROM
        con_contract ct
        WHERE
        ct.tenant_id = b.bp_id
        )
        ) t1
        <where>
            1=1
            <if test="year!=null and year!=''">
                and t1.year = #{year}
            </if>
            <if test="mon!=null and mon!=''">
                and t1.mon = #{mon}
            </if>
            <if test="bpName!=null and bpName!=''">
                and t1.bp_name like concat(concat('%',#{bpName}),'%')
            </if>
            <if test="hostUnitIdN!=null and hostUnitIdN!=''">
                and t1.host_unit_id_n like concat(concat('%',#{hostUnitIdN}),'%')
            </if>
            <if test="economicInduClassifyN!=null and economicInduClassifyN!=''">
                and t1.economic_indu_classify_n like concat(concat('%',#{economicInduClassifyN}),'%')
            </if>
            <if test="hostProjectManagerN!=null and hostProjectManagerN!=''">
                and t1.host_project_manager_n like concat(concat('%',#{hostProjectManagerN}),'%')
            </if>
        </where>

    </select>
    <select id="queryLonAllReport" resultMap="BaseResultMap" parameterType="com.hand.hls.fnd.dto.CockpitImport">
        select tt.* from (SELECT
        t1.YEAR,
        a.bp_id,
        t1.mon,
        a.bp_name,
        (
        (
        SELECT
        sum(nvl(g.FINANCE_AMOUNT, 0))
        FROM
        lon_contract g
        WHERE
        g.CREDIT_BP_ID = a.bp_id
        ) - (
        SELECT
        nvl(sum(y.DUE_AMOUNT), 0)
        FROM
        lon_contract_withdraw y,
        lon_contract d
        WHERE
        y.CONTRACT_ID = d.CONTRACT_ID
        AND d.CREDIT_BP_ID = a.BP_ID
        AND y.WITHDRAW_STATUS = 'APPROVED'
        AND NOT EXISTS (
        SELECT
        1
        FROM
        lon_contract_repayment s
        WHERE
        s.WITHDRAW_ID = y.WITHDRAW_ID
        AND s.write_off_flag = 'NOT'
        )
        )
        ) SUM_DUE_AMOUNT,
        (
        SELECT
        nvl(sum(y.DUE_AMOUNT), 0)
        FROM
        lon_contract_withdraw y,
        lon_contract d
        WHERE
        y.CONTRACT_ID = d.CONTRACT_ID
        AND d.CREDIT_BP_ID = a.BP_ID
        AND y.WITHDRAW_STATUS = 'APPROVED'
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract_repayment s
        WHERE
        s.WITHDRAW_ID = y.WITHDRAW_ID
        AND s.write_off_flag = 'NOT'
        )
        ) SUM_CREDIT_AMOUNT,
        (
        SELECT
        nvl(sum(s.DUE_AMOUNT), 0)
        FROM
        lon_contract_repayment s,
        lon_contract f
        WHERE
        s.CF_ITEM = '301'
        AND s.DUE_DATE >= to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND s.CONTRACT_ID = f.CONTRACT_ID
        AND f.CREDIT_BP_ID = a.BP_ID
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract_withdraw q
        WHERE
        s.WITHDRAW_ID = q.WITHDRAW_ID
        AND q.withdraw_status = 'APPROVED'
        AND q.DUE_DATE &lt; to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) YEAR_INITIAL_AMOUNT,
        (
        SELECT
        nvl(
        sum(cc.PLANNED_DUE_AMOUNT),
        0
        )
        FROM
        lon_con_repayment_batch_ln cc
        WHERE
        cc.repayment_date BETWEEN to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        t1.mon,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        CAST(t1.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract_repayment ccc
        WHERE
        cc.repayment_id = ccc.repayment_id
        AND ccc.cf_item = '301'
        )
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract y
        WHERE
        y.contract_id = cc.contract_id
        AND y.credit_bp_id = a.bp_id
        )
        ) RECEIVED_PRINCIPAL,
        (
        SELECT
        nvl(
        sum(cc.PLANNED_DUE_AMOUNT),
        0
        )
        FROM
        lon_con_repayment_batch_ln cc
        WHERE
        cc.repayment_date BETWEEN to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        t1.mon,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        CAST(t1.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract_repayment ccc
        WHERE
        cc.repayment_id = ccc.repayment_id
        AND ccc.cf_item = '302'
        )
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract y
        WHERE
        y.contract_id = cc.contract_id
        AND y.credit_bp_id = a.bp_id
        )
        ) RECEIVED_INTEREST,
        (
        (
        SELECT
        nvl(
        sum(w.PLANNED_DUE_AMOUNT),
        0
        )
        FROM
        lon_contract_repayment w
        WHERE
        w.cf_item = '301'
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract cc
        WHERE
        cc.contract_id = w.contract_id
        AND cc.credit_bp_id = a.bp_id
        )
        AND w.PLANNED_DUE_DATE > LAST_DAY(
        to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        CAST(t1.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        sum(w.PLANNED_DUE_AMOUNT),
        0
        )
        FROM
        lon_contract_repayment w
        WHERE
        w.cf_item = '301'
        AND w.write_off_flag = 'NOT'
        AND EXISTS (
        SELECT
        1
        FROM
        lon_contract cc
        WHERE
        cc.contract_id = w.contract_id
        AND cc.credit_bp_id = a.bp_id
        )
        AND w.PLANNED_DUE_DATE &lt; to_date(
        concat(
        CAST(t1. YEAR AS CHAR),
        '-',
        t1.mon,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) redisual_amount
        FROM
        hls_bp_master a,
        (
        SELECT
        b. YEAR,
        b.mon
        FROM
        jc_cockpit_import b
        GROUP BY
        b. YEAR,
        b.MON
        ) t1
        where  EXISTS (
        SELECT
        1
        FROM
        lon_contract c
        WHERE
        c.credit_bp_id = a.bp_id
        ))tt
        <where>
            1=1

            <if test="year!=null and year!=''">
                and tt.year = #{year}
            </if>
            <if test="mon!=null and mon!=''">
                and tt.mon = #{mon}
            </if>
            <if test="bpName!=null and bpName!=''">
                and tt.bp_name like concat(concat('%',#{bpName}),'%')
            </if>
            <if test="hostUnitIdN!=null and hostUnitIdN!=''">
                and tt.host_unit_id_n like concat(concat('%',#{hostUnitIdN}),'%')
            </if>
            <if test="economicInduClassifyN!=null and economicInduClassifyN!=''">
                and tt.economic_indu_classify_n like concat(concat('%',#{economicInduClassifyN}),'%')
            </if>
            <if test="hostProjectManagerN!=null and hostProjectManagerN!=''">
                and tt.host_project_manager_n like concat(concat('%',#{hostProjectManagerN}),'%')
            </if>
        </where>

    </select>
    <select id="queryPrjAllReport" resultMap="BaseResultMap" parameterType="com.hand.hls.fnd.dto.CockpitImport">
        SELECT
        t1.*, (
        t1.completed_launch_amount / t1.launch_amount
        ) launch_ratio,
        (
        t1.completed_bearing_amount / t1.bearing_amount
        ) bearing_ratio,
        (
        t1.completed_income_amount / t1.income_amount
        ) income_ratio,
        (
        t1.completed_income_amount - t1.cost_total_amount
        ) completed_profit_amount,
        (
        (
        t1.completed_income_amount - t1.cost_total_amount
        ) / t1.profit_amount
        ) profit_ratio

        FROM
        (
        SELECT
        t. YEAR,
        t.mon,
        t.host_project_manager,
        (
        SELECT
        u.UNIT_NAME
        FROM
        sys_user_allocation sa,
        fnd_employee_assigns ea,
        fnd_org_unit u
        WHERE
        sa.EMPLOYEE_ASSIGN_ID = ea.EMPLOYEE_ASSIGN_ID
        AND ea.UNIT_ID = u.unit_id
        AND sa.user_id = t.host_project_manager
        ) host_unit_id_n,
        (
        SELECT
        s.description
        FROM
        sys_user s
        WHERE
        s.user_id = t.host_project_manager
        ) host_project_manager_n,
        t.launch_amount,
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND cc.host_project_manager = t.host_project_manager
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND c.host_project_manager = t.host_project_manager
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        sum(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.host_project_manager = t.host_project_manager
        AND ac.document_type = 'REFUND'
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) COMPLETED_launch_amount,
        t.BEARING_AMOUNT,
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND cc.host_project_manager = t.host_project_manager
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND c.host_project_manager = t.host_project_manager
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        sum(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.host_project_manager = t.host_project_manager
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.host_project_manager = t.host_project_manager
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.host_project_manager = t.host_project_manager
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        )
        ) COMPLETED_BEARING_AMOUNT,
        t.INCOME_AMOUNT,
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.host_project_manager = t.host_project_manager
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.host_project_manager = t.host_project_manager
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) COMPLETED_INCOME_AMOUNT,
        t.PROFIT_AMOUNT,
        (
        (
        SELECT
        nvl(
        sum(
        nvl(
        (
        SELECT
        nvl(SUM(ccc.PRINCIPAL), 0)
        FROM
        con_contract_cashflow ccc
        WHERE
        ccc.contract_id = c.contract_id
        AND ccc.cf_item IN (1, 11)
        AND ccc.times >= cc.times
        AND ccc.cf_status = 'RELEASE'
        ),
        0
        ) * nvl(
        (
        SELECT
        (
        DATEDIFF(cc.due_date ,MAX(ccf.due_date))
        ) / 360
        FROM
        con_contract_cashflow ccf
        WHERE
        ccf.contract_id = cc.contract_id
        AND ccf.times &lt; cc.times
        AND ccf.cf_item IN (1, 0)
        AND ccf.cf_status = 'RELEASE'
        ),
        0
        ) *  nvl(c.COM_COST_RATE, 0)
        ),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.host_project_manager = t.host_project_manager
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) + (
        SELECT
        nvl(
        SUM(
        nvl(
        (
        SELECT
        nvl(SUM(ccc.NET_PRINCIPAL), 0)
        FROM
        con_contract_cashflow ccc
        WHERE
        ccc.contract_id = c.contract_id
        AND ccc.cf_item IN (1, 11)
        AND ccc.times >= cc.times
        AND ccc.cf_status = 'RELEASE'
        ),
        0
        ) * nvl(
        (
        SELECT
        (
        DATEDIFF(cc.due_date , MAX(ccf.due_date))
        ) / 360
        FROM
        con_contract_cashflow ccf
        WHERE
        ccf.contract_id = cc.contract_id
        AND ccf.times &lt; cc.times
        AND ccf.cf_item IN (1, 0)
        AND ccf.cf_status = 'RELEASE'
        ),
        0
        ) *  nvl(c.COM_COST_RATE, 0)
        ),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.host_project_manager = t.host_project_manager
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        )
        ) cost_total_amount
        FROM
        jc_prj_cockpit_import t
        WHERE
        (
        #{authorizationFlag} = 'Y'
        AND t.host_project_manager = #{hostProjectManager}
        )
        OR (
        #{authorizationFlag} = 'N'
        AND (
        SELECT
        ea.unit_id
        FROM
        sys_user_allocation sa,
        fnd_employee_assigns ea
        WHERE
        ea.EMPLOYEE_ASSIGN_ID = sa.EMPLOYEE_ASSIGN_ID
        AND sa.user_id = t.host_project_manager
        ) in (
        SELECT
        ea.unit_id
        FROM
        sys_user_allocation sa,
        fnd_employee_assigns ea
        WHERE
        ea.EMPLOYEE_ASSIGN_ID = sa.EMPLOYEE_ASSIGN_ID
        AND sa.user_id = #{hostProjectManager}
        )
        )
        OR (#{authorizationFlag} = 'All')
        ) t1
        <where>
            1=1
            <if test="year!=null and year!=''">
                and t1.year = #{year}
            </if>
            <if test="mon!=null and mon!=''">
                and t1.mon = #{mon}
            </if>
            <if test="hostProjectManagerN!=null and hostProjectManagerN!=''">
                and t1.host_project_manager_n like concat(concat('%',#{hostProjectManagerN}),'%')
            </if>
            <if test="hostUnitIdN!=null and hostUnitIdN!=''">
                and t1.host_unit_id_n like concat(concat('%',#{hostUnitIdN}),'%')
            </if>
        </where>
    </select>
    <select id="queryAllReport" resultMap="BaseResultMap" parameterType="com.hand.hls.fnd.dto.CockpitImport">
        SELECT
        t.COCKPIT_ID,
        t. YEAR,
        t.mon,
        t.item,
        t.BUS_ONE_AMOUNT,
        t.BUS_TWO_AMOUNT,
        t.BUS_THREE_AMOUNT,
        t.BUS_TD_AMOUNT,
        t.TOTAL_AMOUNT,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末生息资产规模' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        con_contract cc,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末累计收入' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        END AS COMPLETED_AMOUNT_OD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        con_contract cc,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.bus_one_amount
        WHEN '本月末生息资产规模' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        con_contract cc,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.bus_one_amount
        WHEN '本月末累计收入' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.bus_one_amount
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.bus_one_amount
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_OD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.bus_one_amount
        END AS COMPLETED_RATIO_OD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        con_contract cc,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末生息资产规模' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末累计收入' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        END AS COMPLETED_AMOUNT_TD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.bus_two_amount
        WHEN '本月末生息资产规模' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.bus_two_amount
        WHEN '本月末累计收入' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.bus_two_amount
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.bus_two_amount
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_TD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.bus_two_amount
        END AS COMPLETED_RATIO_TD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末生息资产规模' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末累计收入' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        END AS COMPLETED_AMOUNT_THD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_THREE_AMOUNT
        WHEN '本月末生息资产规模' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_THREE_AMOUNT
        WHEN '本月末累计收入' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_THREE_AMOUNT
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.BUS_THREE_AMOUNT
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_THD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.BUS_THREE_AMOUNT
        END AS COMPLETED_RATIO_THD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末生息资产规模' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末累计收入' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 10
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        END AS COMPLETED_AMOUNT_ZD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND ac.document_type = 'REFUND'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_TD_AMOUNT
        WHEN '本月末生息资产规模' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = cc.HOST_UNIT_ID
        )
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac,
        con_contract c
        WHERE
        ac.contract_id = c.contract_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = c.HOST_UNIT_ID
        )
        AND ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_TD_AMOUNT
        WHEN '本月末累计收入' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_TD_AMOUNT
        WHEN '其中：利息（含税）' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 10
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.BUS_TD_AMOUNT
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.BUS_TD_AMOUNT
        END AS COMPLETED_RATIO_ZD,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.document_type = 'REFUND'
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末生息资产规模' THEN
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '本月末累计收入' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：利息（含税）' THEN
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 10
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        END AS TOTAL_COMPLETED_AMOUNT,
        CASE t.item
        WHEN '本月末累计投放' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.document_type = 'REFUND'
        AND ac.due_date BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.TOTAL_AMOUNT
        WHEN '本月末生息资产规模' THEN
        (
        (
        SELECT
        nvl(
        SUM(cba.ACTUAL_PAYMENT_AMOUNT),
        0
        )
        FROM
        csh_payment_req_hd c,
        csh_payment_req_ln cl,
        csh_pay_req_ln_bank_account  cba,
        con_contract cc
        WHERE
        c.SOURCE_CONTRACT_ID = cc.contract_id
        AND c.payment_type = 'PAYMENT'
        AND c.payment_req_status = 'APPROVED'
        AND cl.PAYMENT_REQ_ID = c.PAYMENT_REQ_ID
        AND cl.PAYMENT_REQ_LN_ID = cba.PAYMENT_REQ_LN_ID
        AND cba.ACTUAL_PAYMENT_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) - (
        SELECT
        nvl(
        SUM(cc.RECEIVED_PRINCIPAL),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'N'
        )
        ) - (
        SELECT
        nvl(SUM(cc.NET_PRINCIPAL), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        AND EXISTS (
        SELECT
        1
        FROM
        prj_quotation q
        WHERE
        q.QUOTATION_ID = c.QUOTATION_ID
        AND q.BONDED_LEASE_FLAG = 'Y'
        )
        ) - (
        SELECT
        nvl(
        SUM(
        cprh.ACTUAL_HD_PAY_AMOUNT * a.PAYMENT_DISCHARGE_RADIO / 100 - a.cash_discharge_amount
        ),
        0
        )
        FROM
        hls_payment_discharge a,
        hls_bill_request b,
        prj_project c,
        csh_payment_req_hd cprh
        WHERE
        a.bill_id = b.BILL_ID
        AND c.PROJECT_ID = b.PROJECT_ID
        AND b.PAYMENT_REQ_ID = cprh.PAYMENT_REQ_ID
        AND b.BILL_TYPE in ('INTERNATIONAL_CREDIT','DOMESTIC_CREDIT')
        AND a.payment_discharge_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(
        SUM(nvl(ac.due_amount, 0)),
        0
        )
        FROM
        jc_adjustment_cu ac
        WHERE
        ac.due_date &lt;= LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.TOTAL_AMOUNT
        WHEN '本月末累计收入' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (3, 10)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.TOTAL_AMOUNT
        WHEN '其中：利息（含税）' THEN
        (
        (
        SELECT
        nvl(
        SUM(cc.RECEIVED_INTEREST),
        0
        )
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item IN (1, 11)
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) + (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 10
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND EXISTS (
        SELECT
        1
        FROM
        fnd_org_unit u
        WHERE
        u.UNIT_CODE = 'BUSINESS_DEPT_ZD'
        AND u.unit_id = p.HOST_UNIT_ID
        )
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        )
        ) / t.TOTAL_AMOUNT
        WHEN '其中：手续费' THEN
        (
        SELECT
        nvl(SUM(cc.RECEIVED_amount), 0)
        FROM
        con_contract c,
        con_contract_cashflow cc,
        prj_project p
        WHERE
        c.contract_id = cc.contract_id
        AND cc.cf_item = 3
        AND cc.cf_status = 'RELEASE'
        AND c.project_id = p.project_id
        AND cc.WRITE_OFF_FLAG = 'FULL'
        AND cc.LAST_RECEIVED_DATE BETWEEN to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        1,
        '-1'
        ),
        'YYYY-MM-DD'
        )
        AND LAST_DAY(
        to_date(
        concat(
        CAST(t. YEAR AS CHAR),
        '-',
        CAST(t.mon AS CHAR),
        '-1'
        ),
        'YYYY-MM-DD'
        )
        )
        ) / t.TOTAL_AMOUNT
        END AS TOTAL_COMPLETED_RATIO
        FROM
        jc_cockpit_import t
        <where>
            1=1
            <if test="year!=null and year!=''">
                and t.year = #{year}
            </if>
            <if test="mon!=null and mon!=''">
                and t.mon = #{mon}
            </if>
            <if test="item!=null and item!=''">
                and t.item like concat(concat('%',#{item}),'%')
            </if>
        </where>
    </select>


</mapper>
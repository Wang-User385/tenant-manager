<?xml version="1.0" encoding="UTF-8"?>
<!--
    $Author: hxh
    $Date: 2020年8月20日
    $Revision: 1.0
    $Purpose: 存续期弹窗界面解押、回购 共用
-->
<a:screen xmlns:a="http://www.leaf-framework.org/application" customizationEnabled="true" dynamiccreateenabled="true"
          trace="true">
    <a:view>
        <a:link id="hls900_quotation_fee_save_link_id" url="${/request/@context_path}/prj/quotation/deposit/fee/save"/>

        <script><![CDATA[


        window['${/parameter/@bp_seq}${/parameter/@layout_code}_dynamic_link_renderer'] = function(value, record, name, config_record, bp_seq) {
            var link_function = '';
            window['${/parameter/@bp_seq}${/parameter/@layout_code}_hls_link_render_record'][record.id + '---' + name] = record;
            if (name == 'due_date') {
                if(record.get('overdue_flag') == 'Y'){
                    return '<div style="background: #9f9f9f"">' +value+ '</div>';
                }else{
                    //return '<div style="background: #00AA00">' +value+ '</div>';
                    return value;
                }

            }
        };


        //查询时调用(grid,table,gridBox)
        window['${/parameter/@bp_seq}${/parameter/@layout_code}_on_layout_dynamic_grid_query'] = function (ds, qpara, bp_seq) {

            debugger;
            qpara['project_id'] = '${/parameter/@project_id}';

            ds.queryurl = ds.queryurl + '&wflRuleControlFlag=Y';

        };




        ]]></script>
        <a:screen-include screen="modules/COMMON/LAYOUT/hls_common_get_layout_code.lview"/>
    </a:view>
</a:screen>



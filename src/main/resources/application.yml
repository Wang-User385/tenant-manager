server:
  port: ${PORT:8080}

spring:
  datasource:
    # 建议格式：jdbc:postgresql://主机:端口/数据库名
    url: jdbc:postgresql://dpg-d859g0t8nd3s73dia580-a.oregon-postgres.render.com:5432/lm_tenent?sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory
    username: lm_tenent
    password: H5eLH9JG8eWhO91eel5tu9Aa2jnvfOer
    driver-class-name: org.postgresql.Driver
    # 新增：HikariCP 连接池优化配置
    hikari:
      max-lifetime: 1800000  # 连接最大存活时间 30 分钟（小于 Render 数据库超时时间）
      idle-timeout: 600000   # 空闲连接超时时间 10 分钟
      connection-timeout: 30000 # 获取连接超时时间 30 秒
      validation-timeout: 5000  # 连接验证超时时间 5 秒
      keepalive-time: 300000    # 保活心跳时间 5 分钟
      minimum-idle: 5           # 最小空闲连接数
      maximum-pool-size: 10     # 最大连接池大小

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    com.hand.hls.hls: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

server:
  port: ${PORT:8080}

spring:
  datasource:
    # 建议格式：jdbc:postgresql://主机:端口/数据库名
    url: jdbc:postgresql://dpg-d859g0t8nd3s73dia580-a.oregon-postgres.render.com:5432/lm_tenent
    username: lm_tenent
    password: H5eLH9JG8eWhO91eel5tu9Aa2jnvfOer
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    com.hand.hls.hls: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
